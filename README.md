# Simple File Manager

This is a Simple file manager that behaves like a mini database like mongo, adds id automatically.

It can: 
- Save objets
- Get object by id
- Get all elements
- Remove item by id
- Remove all items
